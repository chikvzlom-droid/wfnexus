# Backend API URL (без /api/v1). В dev проксируется через Vite, необязателен.
# В продакшене (Vercel) установите URL вашего backend, например:
# VITE_API_URL=https://warframe-nexus-api.onrender.com/api/v1
